# 테이블 명세서

과제명: 오픈소스 LLM·RAG 기반 PDF 양식 자동 작성 시스템 (뚝딱, TookTak)
2026. 06. 22.

---

## Ⅰ. 테이블 목록

| 테이블명 | 테이블_ID | 비고 |
|---|---|---|
| 사용자 | USERS | 시스템 사용자 계정 정보 |
| 빈 양식 | FORMS | 업로드된 PDF 양식 템플릿 |
| 폼필드 | FORM_FIELDS | 양식에서 추출한 입력 필드 |
| 지식 문서 | KNOWLEDGE_DOCUMENTS | RAG 근거가 되는 사내 원본 문서 |
| 지식 청크 | KNOWLEDGE_CHUNKS | 문서를 분할한 텍스트 청크 (벡터는 ChromaDB) |
| 생성 문서 | DOCUMENTS | 양식을 채운 생성 작업 |
| 채운 필드 | FILLED_FIELDS | 문서별 생성된 필드 값 |
| 근거 연결 | DOCUMENT_EVIDENCE | 채운 값 ↔ 근거 청크 (M:N) |
| KPI 평가 | EVAL_RESULTS | KPI 측정 결과 |

---

## Ⅱ. 테이블 정의서

### 사용자 (USERS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 사용자 (USERS) · **테이블 ID** USERS
**테이블설명** 시스템에 접근하는 사용자 계정 정보를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 사용자 ID | user_id | INT | | Y | Y | | | | AUTO_INCREMENT |
| 이메일 | email | VARCHAR | 255 | Y | | | Y | | |
| 이름 | name | VARCHAR | 100 | | | | | | |
| 생성일시 | created_at | DATETIME | | | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE USERS (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(255) NOT NULL UNIQUE,
    name VARCHAR(100),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 빈 양식 (FORMS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 빈 양식 (FORMS) · **테이블 ID** FORMS
**테이블설명** 사용자가 업로드한 빈 PDF 양식 템플릿을 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 양식 ID | form_id | VARCHAR | 32 | Y | Y | | | | UUID |
| 사용자 ID | user_id | INT | | | | Y | | | 업로드 사용자 |
| 파일명 | filename | VARCHAR | 255 | Y | | | | | |
| 페이지수 | page_count | INT | | | | | | | 기본 1 |
| AcroForm 여부 | is_form | TINYINT | 1 | | | | | | 기본 1 |
| 저장경로 | src_path | VARCHAR | 500 | | | | | | |
| 업로드일시 | created_at | DATETIME | | | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE FORMS (
    form_id VARCHAR(32) PRIMARY KEY,
    user_id INT,
    filename VARCHAR(255) NOT NULL,
    page_count INT DEFAULT 1,
    is_form TINYINT(1) DEFAULT 1,
    src_path VARCHAR(500),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES USERS(user_id)
);
```

### 폼필드 (FORM_FIELDS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 폼필드 (FORM_FIELDS) · **테이블 ID** FORM_FIELDS
**테이블설명** 양식에서 추출한 입력 필드(이름·라벨·타입·좌표)를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 필드 ID | field_id | INT | | Y | Y | | | | AUTO_INCREMENT |
| 양식 ID | form_id | VARCHAR | 32 | Y | | Y | | | |
| 필드명 | name | VARCHAR | 100 | Y | | | Y | | (form_id,name) 복합 UK |
| 라벨 | label | VARCHAR | 255 | | | | | | 추론된 라벨 |
| 타입 | field_type | VARCHAR | 20 | | | | | | text/checkbox/radio |
| 페이지 | page | INT | | | | | | | |
| 좌표 x0 | bbox_x0 | DECIMAL | 8,2 | | | | | | |
| 좌표 y0 | bbox_y0 | DECIMAL | 8,2 | | | | | | |
| 좌표 x1 | bbox_x1 | DECIMAL | 8,2 | | | | | | |
| 좌표 y1 | bbox_y1 | DECIMAL | 8,2 | | | | | | |
| 필수여부 | required | TINYINT | 1 | | | | | | |
| 최대길이 | max_length | INT | | | | | | | |

```sql
CREATE TABLE FORM_FIELDS (
    field_id INT AUTO_INCREMENT PRIMARY KEY,
    form_id VARCHAR(32) NOT NULL,
    name VARCHAR(100) NOT NULL,
    label VARCHAR(255),
    field_type VARCHAR(20) DEFAULT 'text',
    page INT DEFAULT 0,
    bbox_x0 DECIMAL(8,2), bbox_y0 DECIMAL(8,2),
    bbox_x1 DECIMAL(8,2), bbox_y1 DECIMAL(8,2),
    required TINYINT(1) DEFAULT 0,
    max_length INT,
    UNIQUE KEY uq_form_field (form_id, name),
    FOREIGN KEY (form_id) REFERENCES FORMS(form_id)
);
```

### 지식 문서 (KNOWLEDGE_DOCUMENTS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 지식 문서 (KNOWLEDGE_DOCUMENTS) · **테이블 ID** KNOWLEDGE_DOCUMENTS
**테이블설명** RAG 근거가 되는 사내 원본 문서를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 문서 ID | doc_id | INT | | Y | Y | | | | AUTO_INCREMENT |
| 출처 | source | VARCHAR | 255 | Y | | | | | 파일명 |
| 제목 | title | VARCHAR | 255 | | | | | | |
| 글자수 | char_count | INT | | | | | | | |
| 인덱싱일시 | indexed_at | DATETIME | | | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE KNOWLEDGE_DOCUMENTS (
    doc_id INT AUTO_INCREMENT PRIMARY KEY,
    source VARCHAR(255) NOT NULL,
    title VARCHAR(255),
    char_count INT DEFAULT 0,
    indexed_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 지식 청크 (KNOWLEDGE_CHUNKS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 지식 청크 (KNOWLEDGE_CHUNKS) · **테이블 ID** KNOWLEDGE_CHUNKS
**테이블설명** 문서를 분할한 텍스트 청크를 관리한다. (임베딩 벡터는 ChromaDB에 별도 저장)

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 청크 ID | chunk_id | VARCHAR | 32 | Y | Y | | | | 해시 |
| 문서 ID | doc_id | INT | | Y | | Y | | | |
| 순번 | chunk_index | INT | | Y | | | | | |
| 본문 | text | TEXT | | | | | | | |
| 글자수 | char_count | INT | | | | | | | |

```sql
CREATE TABLE KNOWLEDGE_CHUNKS (
    chunk_id VARCHAR(32) PRIMARY KEY,
    doc_id INT NOT NULL,
    chunk_index INT NOT NULL,
    text TEXT,
    char_count INT DEFAULT 0,
    FOREIGN KEY (doc_id) REFERENCES KNOWLEDGE_DOCUMENTS(doc_id)
);
```

### 생성 문서 (DOCUMENTS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 생성 문서 (DOCUMENTS) · **테이블 ID** DOCUMENTS
**테이블설명** 양식을 채운 생성 작업과 입력 컨셉·결과를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 문서 ID | document_id | INT | | Y | Y | | | | AUTO_INCREMENT |
| 양식 ID | form_id | VARCHAR | 32 | Y | | Y | | | |
| 사용자 ID | user_id | INT | | | | Y | | | |
| 컨셉 | concept | TEXT | | | | | | | 사용자 입력 |
| 모델 | model | VARCHAR | 50 | | | | | | qwen3:8b |
| 근거율 | grounded_ratio | DECIMAL | 4,3 | | | | | | |
| 상태 | status | VARCHAR | 20 | | | | | | generated/filled/downloaded |
| 출력경로 | output_path | VARCHAR | 500 | | | | | | |
| 생성일시 | created_at | DATETIME | | | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE DOCUMENTS (
    document_id INT AUTO_INCREMENT PRIMARY KEY,
    form_id VARCHAR(32) NOT NULL,
    user_id INT,
    concept TEXT,
    model VARCHAR(50) DEFAULT 'qwen3:8b',
    grounded_ratio DECIMAL(4,3),
    status VARCHAR(20) DEFAULT 'generated',
    output_path VARCHAR(500),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (form_id) REFERENCES FORMS(form_id),
    FOREIGN KEY (user_id) REFERENCES USERS(user_id)
);
```

### 채운 필드 (FILLED_FIELDS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 채운 필드 (FILLED_FIELDS) · **테이블 ID** FILLED_FIELDS
**테이블설명** 생성 문서별로 채운 필드 값과 근거 여부를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 채운값 ID | filled_id | INT | | Y | Y | | | | AUTO_INCREMENT |
| 문서 ID | document_id | INT | | Y | | Y | | | |
| 필드명 | field_name | VARCHAR | 100 | Y | | | Y | | (document_id,field_name) 복합 UK |
| 값 | value | TEXT | | | | | | | |
| 근거여부 | grounded | TINYINT | 1 | | | | | | |
| 신뢰도 | confidence | DECIMAL | 4,3 | | | | | | |

```sql
CREATE TABLE FILLED_FIELDS (
    filled_id INT AUTO_INCREMENT PRIMARY KEY,
    document_id INT NOT NULL,
    field_name VARCHAR(100) NOT NULL,
    value TEXT,
    grounded TINYINT(1) DEFAULT 0,
    confidence DECIMAL(4,3),
    UNIQUE KEY uq_doc_field (document_id, field_name),
    FOREIGN KEY (document_id) REFERENCES DOCUMENTS(document_id)
);
```

### 근거 연결 (DOCUMENT_EVIDENCE)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 근거 연결 (DOCUMENT_EVIDENCE) · **테이블 ID** DOCUMENT_EVIDENCE
**테이블설명** 채운 값과 근거가 된 지식 청크의 다대다(M:N) 관계를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 채운값 ID | filled_id | INT | | Y | Y | Y | | | filled_fields 참조 |
| 청크 ID | chunk_id | VARCHAR | 32 | Y | Y | Y | | | knowledge_chunks 참조 |
| 유사도 점수 | score | DECIMAL | 5,4 | | | | | | |

```sql
CREATE TABLE DOCUMENT_EVIDENCE (
    filled_id INT NOT NULL,
    chunk_id VARCHAR(32) NOT NULL,
    score DECIMAL(5,4),
    PRIMARY KEY (filled_id, chunk_id),
    FOREIGN KEY (filled_id) REFERENCES FILLED_FIELDS(filled_id),
    FOREIGN KEY (chunk_id) REFERENCES KNOWLEDGE_CHUNKS(chunk_id)
);
```

### KPI 평가 (EVAL_RESULTS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** KPI 평가 (EVAL_RESULTS) · **테이블 ID** EVAL_RESULTS
**테이블설명** KPI 측정 결과(정확도·근거율·시간단축·외부호출)를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 평가 ID | eval_id | INT | | Y | Y | | | | AUTO_INCREMENT |
| 데이터셋 | dataset | VARCHAR | 50 | Y | | | | | |
| 정확도 | accuracy | DECIMAL | 4,3 | | | | | | |
| 시간단축 | time_reduction | DECIMAL | 4,3 | | | | | | |
| 근거율 | grounded_ratio | DECIMAL | 4,3 | | | | | | |
| 외부호출수 | external_calls | INT | | | | | | | |
| 측정일시 | measured_at | DATETIME | | | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE EVAL_RESULTS (
    eval_id INT AUTO_INCREMENT PRIMARY KEY,
    dataset VARCHAR(50) NOT NULL,
    accuracy DECIMAL(4,3),
    time_reduction DECIMAL(4,3),
    grounded_ratio DECIMAL(4,3),
    external_calls INT DEFAULT 0,
    measured_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```
