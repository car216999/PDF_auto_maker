# 테이블 명세서

과제명: 오픈소스 LLM·RAG 기반 PDF 양식 자동 작성 시스템 (뚝딱, TookTak)
DDL 원본: [`erd.txt`](erd.txt) · 표기 — PK: 기본키, FK: 외래키, NN: NOT NULL, U: UNIQUE

---

### TBL-01 · users

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | users | **테이블 ID** | TBL-01 |
| **설명** | 사용자 | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| user_id | INT | PK | NN | AUTO_INCREMENT | 사용자 ID |
| email | VARCHAR(255) | U | NN | | 이메일 |
| name | VARCHAR(100) | | Y | | 이름 |
| created_at | DATETIME | | Y | CURRENT_TIMESTAMP | 가입일시 |

**Table 생성 스크립트**

```sql
CREATE TABLE users (
    user_id     INT AUTO_INCREMENT PRIMARY KEY,
    email       VARCHAR(255) NOT NULL UNIQUE,
    name        VARCHAR(100),
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-02 · forms

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | forms | **테이블 ID** | TBL-02 |
| **설명** | 빈 양식 (업로드된 PDF 템플릿) | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| form_id | VARCHAR(32) | PK | NN | | 양식 ID(UUID) |
| user_id | INT | FK→users | Y | | 업로드 사용자 |
| filename | VARCHAR(255) | | NN | | 원본 파일명 |
| page_count | INT | | Y | 1 | 페이지 수 |
| is_form | TINYINT(1) | | Y | 1 | AcroForm 여부 |
| src_path | VARCHAR(500) | | Y | | 원본 저장 경로 |
| created_at | DATETIME | | Y | CURRENT_TIMESTAMP | 업로드 일시 |

**Table 생성 스크립트**

```sql
CREATE TABLE forms (
    form_id     VARCHAR(32) PRIMARY KEY,
    user_id     INT,
    filename    VARCHAR(255) NOT NULL,
    page_count  INT DEFAULT 1,
    is_form     TINYINT(1) DEFAULT 1,
    src_path    VARCHAR(500),
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-03 · form_fields

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | form_fields | **테이블 ID** | TBL-03 |
| **설명** | 폼필드 (파싱 산출물) | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| field_id | INT | PK | NN | AUTO_INCREMENT | 필드 ID |
| form_id | VARCHAR(32) | FK→forms | NN | | 양식 ID |
| name | VARCHAR(100) | U* | NN | | AcroForm 필드명 |
| label | VARCHAR(255) | | Y | | 추론된 라벨 |
| field_type | VARCHAR(20) | | Y | 'text' | text/checkbox/radio/choice |
| page | INT | | Y | 0 | 페이지 인덱스 |
| bbox_x0 | DECIMAL(8,2) | | Y | | 좌표 x0 |
| bbox_y0 | DECIMAL(8,2) | | Y | | 좌표 y0 |
| bbox_x1 | DECIMAL(8,2) | | Y | | 좌표 x1 |
| bbox_y1 | DECIMAL(8,2) | | Y | | 좌표 y1 |
| required | TINYINT(1) | | Y | 0 | 필수 여부 |
| max_length | INT | | Y | | 최대 길이 |

\* 복합 UNIQUE: (form_id, name)

**Table 생성 스크립트**

```sql
CREATE TABLE form_fields (
    field_id    INT AUTO_INCREMENT PRIMARY KEY,
    form_id     VARCHAR(32) NOT NULL,
    name        VARCHAR(100) NOT NULL,
    label       VARCHAR(255),
    field_type  VARCHAR(20) DEFAULT 'text',
    page        INT DEFAULT 0,
    bbox_x0     DECIMAL(8,2),
    bbox_y0     DECIMAL(8,2),
    bbox_x1     DECIMAL(8,2),
    bbox_y1     DECIMAL(8,2),
    required    TINYINT(1) DEFAULT 0,
    max_length  INT,
    UNIQUE KEY uq_form_field (form_id, name),
    FOREIGN KEY (form_id) REFERENCES forms (form_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-04 · knowledge_documents

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | knowledge_documents | **테이블 ID** | TBL-04 |
| **설명** | 지식 원본 문서 | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| doc_id | INT | PK | NN | AUTO_INCREMENT | 문서 ID |
| source | VARCHAR(255) | | NN | | 출처 파일명 |
| title | VARCHAR(255) | | Y | | 제목 |
| char_count | INT | | Y | 0 | 글자 수 |
| indexed_at | DATETIME | | Y | CURRENT_TIMESTAMP | 인덱싱 일시 |

**Table 생성 스크립트**

```sql
CREATE TABLE knowledge_documents (
    doc_id      INT AUTO_INCREMENT PRIMARY KEY,
    source      VARCHAR(255) NOT NULL,
    title       VARCHAR(255),
    char_count  INT DEFAULT 0,
    indexed_at  DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-05 · knowledge_chunks

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | knowledge_chunks | **테이블 ID** | TBL-05 |
| **설명** | 지식 청크 (임베딩 벡터는 ChromaDB) | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| chunk_id | VARCHAR(32) | PK | NN | | 청크 ID(해시) |
| doc_id | INT | FK→knowledge_documents | NN | | 원본 문서 ID |
| chunk_index | INT | | NN | | 청크 순번 |
| text | TEXT | | Y | | 청크 본문 |
| char_count | INT | | Y | 0 | 글자 수 |

**Table 생성 스크립트**

```sql
CREATE TABLE knowledge_chunks (
    chunk_id    VARCHAR(32) PRIMARY KEY,
    doc_id      INT NOT NULL,
    chunk_index INT NOT NULL,
    text        TEXT,
    char_count  INT DEFAULT 0,
    FOREIGN KEY (doc_id) REFERENCES knowledge_documents (doc_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-06 · documents

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | documents | **테이블 ID** | TBL-06 |
| **설명** | 생성 문서 (채운 문서/작업) | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| document_id | INT | PK | NN | AUTO_INCREMENT | 문서(작업) ID |
| form_id | VARCHAR(32) | FK→forms | NN | | 대상 양식 |
| user_id | INT | FK→users | Y | | 요청 사용자 |
| concept | TEXT | | Y | | 사용자 입력 컨셉 |
| model | VARCHAR(50) | | Y | 'qwen3:8b' | 사용 LLM |
| grounded_ratio | DECIMAL(4,3) | | Y | | 근거 일치율 |
| status | VARCHAR(20) | | Y | 'generated' | generated/filled/downloaded |
| output_path | VARCHAR(500) | | Y | | 완성 PDF 경로 |
| created_at | DATETIME | | Y | CURRENT_TIMESTAMP | 생성 일시 |

**Table 생성 스크립트**

```sql
CREATE TABLE documents (
    document_id    INT AUTO_INCREMENT PRIMARY KEY,
    form_id        VARCHAR(32) NOT NULL,
    user_id        INT,
    concept        TEXT,
    model          VARCHAR(50) DEFAULT 'qwen3:8b',
    grounded_ratio DECIMAL(4,3),
    status         VARCHAR(20) DEFAULT 'generated',
    output_path    VARCHAR(500),
    created_at     DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (form_id) REFERENCES forms (form_id),
    FOREIGN KEY (user_id) REFERENCES users (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-07 · filled_fields

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | filled_fields | **테이블 ID** | TBL-07 |
| **설명** | 채운 필드 값 (생성 산출물) | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| filled_id | INT | PK | NN | AUTO_INCREMENT | 채운값 ID |
| document_id | INT | FK→documents | NN | | 문서 ID |
| field_name | VARCHAR(100) | U* | NN | | 필드명 |
| value | TEXT | | Y | | 채운 값 |
| grounded | TINYINT(1) | | Y | 0 | 근거 존재 여부 |
| confidence | DECIMAL(4,3) | | Y | | 신뢰도 |

\* 복합 UNIQUE: (document_id, field_name)

**Table 생성 스크립트**

```sql
CREATE TABLE filled_fields (
    filled_id   INT AUTO_INCREMENT PRIMARY KEY,
    document_id INT NOT NULL,
    field_name  VARCHAR(100) NOT NULL,
    value       TEXT,
    grounded    TINYINT(1) DEFAULT 0,
    confidence  DECIMAL(4,3),
    UNIQUE KEY uq_doc_field (document_id, field_name),
    FOREIGN KEY (document_id) REFERENCES documents (document_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-08 · document_evidence

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | document_evidence | **테이블 ID** | TBL-08 |
| **설명** | 채운 값 ↔ 근거 청크 (M:N) | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| filled_id | INT | PK, FK→filled_fields | NN | | 채운값 ID |
| chunk_id | VARCHAR(32) | PK, FK→knowledge_chunks | NN | | 근거 청크 ID |
| score | DECIMAL(5,4) | | Y | | 유사도 점수 |

\* 복합 PK: (filled_id, chunk_id)

**Table 생성 스크립트**

```sql
CREATE TABLE document_evidence (
    filled_id   INT NOT NULL,
    chunk_id    VARCHAR(32) NOT NULL,
    score       DECIMAL(5,4),
    PRIMARY KEY (filled_id, chunk_id),
    FOREIGN KEY (filled_id) REFERENCES filled_fields (filled_id),
    FOREIGN KEY (chunk_id) REFERENCES knowledge_chunks (chunk_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

### TBL-09 · eval_results

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|
| **테이블명** | eval_results | **테이블 ID** | TBL-09 |
| **설명** | KPI 평가 결과 | | |

**컬럼 명세**

| 컬럼명 | 타입 | 키 | NULL | 기본값 | 설명 |
|---|---|:---:|:---:|---|---|
| eval_id | INT | PK | NN | AUTO_INCREMENT | 평가 ID |
| dataset | VARCHAR(50) | | NN | | 데이터셋명 |
| accuracy | DECIMAL(4,3) | | Y | | 채움 정확도 |
| time_reduction | DECIMAL(4,3) | | Y | | 시간 단축률 |
| grounded_ratio | DECIMAL(4,3) | | Y | | 근거 일치율 |
| external_calls | INT | | Y | 0 | 외부 호출 수 |
| measured_at | DATETIME | | Y | CURRENT_TIMESTAMP | 측정 일시 |

**Table 생성 스크립트**

```sql
CREATE TABLE eval_results (
    eval_id        INT AUTO_INCREMENT PRIMARY KEY,
    dataset        VARCHAR(50) NOT NULL,
    accuracy       DECIMAL(4,3),
    time_reduction DECIMAL(4,3),
    grounded_ratio DECIMAL(4,3),
    external_calls INT DEFAULT 0,
    measured_at    DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 관계 요약

| 부모 | 자식 | 관계 |
|---|---|:---:|
| users | forms | 1:N |
| forms | form_fields | 1:N |
| forms | documents | 1:N |
| users | documents | 1:N |
| documents | filled_fields | 1:N |
| knowledge_documents | knowledge_chunks | 1:N |
| filled_fields ↔ knowledge_chunks | document_evidence | N:M |
