# 테이블 명세서

과제명: 오픈소스 LLM·RAG 기반 PDF 양식 자동 작성 시스템 (뚝딱, TookTak)
2026. 06. 22. · DBMS: PostgreSQL (실전DB멘토링 반영) · DDL: [erd.txt](erd.txt)

---

## Ⅰ. 테이블 목록

| 테이블명 | 테이블_ID | 비고 |
|---|---|---|
| 사용자 | USERS | 시스템 사용자 계정 정보 |
| PDF 파일 | FORMS | 업로드된 PDF 양식 |
| 폼 필드 | FORM_FIELDS | 양식에서 추출한 입력 필드 |
| 지식 문서 | KNOWLEDGE_DOCUMENTS | RAG 근거가 되는 사내 원본 문서 |
| 지식 청크 | KNOWLEDGE_CHUNKS | 문서를 분할한 텍스트 청크 (벡터는 ChromaDB) |
| 생성 문서 | DOCUMENTS | 양식을 채운 생성 작업 |
| 채운 필드 | FILLED_FIELDS | 문서별 생성된 필드 값 |
| 근거 연결 | DOCUMENT_EVIDENCE | 채운 값 ↔ 근거 청크 (M:N) |
| 평가 결과 | EVAL_RESULTS | KPI 측정 결과 |

---

## Ⅱ. 테이블 정의서

### 사용자 (USERS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 사용자 (USERS) · **테이블 ID** USERS
**테이블설명** 시스템에 접근하는 사용자 계정 정보를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 사용자 ID | user_id | INT | | Y | Y | | | | IDENTITY |
| 이메일 | email | VARCHAR | 255 | Y | | | | | |
| 이름 | name | VARCHAR | 100 | Y | | | | | |
| 가입일시 | created_at | TIMESTAMPTZ | | Y | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE users (
    user_id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    email VARCHAR(255) NOT NULL,
    name VARCHAR(100) NOT NULL,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP NOT NULL,
    PRIMARY KEY (user_id)
);
```

### PDF 파일 (FORMS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** PDF 파일 (FORMS) · **테이블 ID** FORMS
**테이블설명** 사용자가 업로드한 PDF 양식 파일을 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 양식 ID | form_id | VARCHAR | 32 | Y | Y | | | | UUID |
| 업로드 사용자 | user_id | INT | | | | Y | | | NULL 허용 |
| 원본 파일명 | filename | VARCHAR | 255 | Y | | | | | |
| 페이지 수 | page_count | INT | | Y | | | | | DEFAULT 1 |
| AcroForm 여부 | is_form | SMALLINT | | Y | | | | | DEFAULT 1 |
| 원본 저장 경로 | src_path | VARCHAR | 500 | Y | | | | | |
| 업로드 일시 | created_at | TIMESTAMPTZ | | Y | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE forms (
    form_id VARCHAR(32) NOT NULL,
    user_id INT NULL,
    filename VARCHAR(255) NOT NULL,
    page_count INT DEFAULT 1 NOT NULL,
    is_form SMALLINT DEFAULT 1 NOT NULL,
    src_path VARCHAR(500) NOT NULL,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP NOT NULL,
    PRIMARY KEY (form_id),
    FOREIGN KEY (user_id) REFERENCES users (user_id)
);
```

### 폼 필드 (FORM_FIELDS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 폼 필드 (FORM_FIELDS) · **테이블 ID** FORM_FIELDS
**테이블설명** 양식에서 추출한 입력 필드(이름·라벨·타입·좌표)를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 필드 ID | field_id | INT | | Y | Y | | | | IDENTITY |
| 양식 ID | form_id | VARCHAR | 32 | Y | | Y | | | |
| AcroForm 필드명 | name | VARCHAR | 100 | Y | | | Y | | uq_form_field(form_id,name) |
| 추론된 라벨 | label | VARCHAR | 255 | Y | | | | | |
| 필드 타입 | field_type | VARCHAR | 20 | Y | | | | | DEFAULT 'text' |
| 페이지 인덱스 | page | INT | | Y | | | | | DEFAULT 0 |
| 좌표 x0 | bbox_x0 | DECIMAL | 8,2 | Y | | | | | |
| 좌표 y0 | bbox_y0 | DECIMAL | 8,2 | Y | | | | | |
| 좌표 x1 | bbox_x1 | DECIMAL | 8,2 | Y | | | | | |
| 좌표 y1 | bbox_y1 | DECIMAL | 8,2 | Y | | | | | |
| 필수 여부 | required | SMALLINT | | Y | | | | | DEFAULT 0 |
| 최대 길이 | max_length | INT | | Y | | | | | |

```sql
CREATE TABLE form_fields (
    field_id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    form_id VARCHAR(32) NOT NULL,
    name VARCHAR(100) NOT NULL,
    label VARCHAR(255) NOT NULL,
    field_type VARCHAR(20) DEFAULT 'text' NOT NULL,
    page INT DEFAULT 0 NOT NULL,
    bbox_x0 DECIMAL(8,2) NOT NULL, bbox_y0 DECIMAL(8,2) NOT NULL,
    bbox_x1 DECIMAL(8,2) NOT NULL, bbox_y1 DECIMAL(8,2) NOT NULL,
    required SMALLINT DEFAULT 0 NOT NULL,
    max_length INT NOT NULL,
    PRIMARY KEY (field_id),
    FOREIGN KEY (form_id) REFERENCES forms (form_id)
);
CREATE UNIQUE INDEX uq_form_field ON form_fields(form_id, name);
```

### 지식 문서 (KNOWLEDGE_DOCUMENTS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 지식 문서 (KNOWLEDGE_DOCUMENTS) · **테이블 ID** KNOWLEDGE_DOCUMENTS
**테이블설명** RAG 근거가 되는 사내 원본 문서를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 문서 ID | doc_id | INT | | Y | Y | | | | IDENTITY |
| 출처 파일명 | source | VARCHAR | 255 | Y | | | | | |
| 제목 | title | VARCHAR | 255 | Y | | | | | |
| 글자 수 | char_count | INT | | Y | | | | | DEFAULT 0 |
| 인덱싱 일시 | indexed_at | TIMESTAMPTZ | | Y | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE knowledge_documents (
    doc_id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    source VARCHAR(255) NOT NULL,
    title VARCHAR(255) NOT NULL,
    char_count INT DEFAULT 0 NOT NULL,
    indexed_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP NOT NULL,
    PRIMARY KEY (doc_id)
);
```

### 지식 청크 (KNOWLEDGE_CHUNKS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 지식 청크 (KNOWLEDGE_CHUNKS) · **테이블 ID** KNOWLEDGE_CHUNKS
**테이블설명** 문서를 분할한 텍스트 청크를 관리한다. (임베딩 벡터는 ChromaDB에 별도 저장)

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 청크 ID | chunk_id | VARCHAR | 32 | Y | Y | | | | 해시 |
| 원본 문서 ID | doc_id | INT | | Y | | Y | | | |
| 청크 순번 | chunk_index | INT | | Y | | | | | |
| 청크 본문 | text | TEXT | | Y | | | | | |
| 글자 수 | char_count | INT | | Y | | | | | DEFAULT 0 |

```sql
CREATE TABLE knowledge_chunks (
    chunk_id VARCHAR(32) NOT NULL,
    doc_id INT NOT NULL,
    chunk_index INT NOT NULL,
    text TEXT NOT NULL,
    char_count INT DEFAULT 0 NOT NULL,
    PRIMARY KEY (chunk_id),
    FOREIGN KEY (doc_id) REFERENCES knowledge_documents (doc_id)
);
```

### 생성 문서 (DOCUMENTS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 생성 문서 (DOCUMENTS) · **테이블 ID** DOCUMENTS
**테이블설명** 양식을 채운 생성 작업과 입력 컨셉·결과를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 문서(작업) ID | document_id | INT | | Y | Y | | | | IDENTITY |
| 대상 양식 | form_id | VARCHAR | 32 | Y | | Y | | | |
| 요청 사용자 | user_id | INT | | Y | | Y | | | |
| 사용자 입력 컨셉 | concept | TEXT | | Y | | | | | |
| 사용 LLM | model | VARCHAR | 50 | Y | | | | | DEFAULT 'qwen3:8b' |
| 근거 일치율 | grounded_ratio | DECIMAL | 4,3 | Y | | | | | |
| 상태 | status | VARCHAR | 20 | Y | | | | | generated/filled/downloaded |
| 완성 PDF 경로 | output_path | VARCHAR | 500 | Y | | | | | |
| 생성 일시 | created_at | TIMESTAMPTZ | | Y | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE documents (
    document_id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    form_id VARCHAR(32) NOT NULL,
    user_id INT NOT NULL,
    concept TEXT NOT NULL,
    model VARCHAR(50) DEFAULT 'qwen3:8b' NOT NULL,
    grounded_ratio DECIMAL(4,3) NOT NULL,
    status VARCHAR(20) DEFAULT 'generated' NOT NULL,
    output_path VARCHAR(500) NOT NULL,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP NOT NULL,
    PRIMARY KEY (document_id),
    FOREIGN KEY (form_id) REFERENCES forms (form_id),
    FOREIGN KEY (user_id) REFERENCES users (user_id)
);
```

### 채운 필드 (FILLED_FIELDS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 채운 필드 (FILLED_FIELDS) · **테이블 ID** FILLED_FIELDS
**테이블설명** 생성 문서별로 채운 필드 값과 근거 여부를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 채운값 ID | filled_id | INT | | Y | Y | | | | IDENTITY |
| 문서 ID | document_id | INT | | Y | | Y | | | |
| 필드명 | field_name | VARCHAR | 100 | Y | | | Y | | uq_doc_field(document_id,field_name) |
| 채운 값 | value | TEXT | | Y | | | | | |
| 근거 존재 여부 | grounded | SMALLINT | | Y | | | | | DEFAULT 0 |
| 신뢰도 | confidence | DECIMAL | 4,3 | Y | | | | | |

```sql
CREATE TABLE filled_fields (
    filled_id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    document_id INT NOT NULL,
    field_name VARCHAR(100) NOT NULL,
    value TEXT NOT NULL,
    grounded SMALLINT DEFAULT 0 NOT NULL,
    confidence DECIMAL(4,3) NOT NULL,
    PRIMARY KEY (filled_id),
    FOREIGN KEY (document_id) REFERENCES documents (document_id)
);
CREATE UNIQUE INDEX uq_doc_field ON filled_fields(document_id, field_name);
```

### 근거 연결 (DOCUMENT_EVIDENCE)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 근거 연결 (DOCUMENT_EVIDENCE) · **테이블 ID** DOCUMENT_EVIDENCE
**테이블설명** 채운 값과 근거가 된 지식 청크의 다대다(M:N) 관계를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 채운값 ID | filled_id | INT | | Y | Y | Y | | | filled_fields 참조 |
| 근거 청크 ID | chunk_id | VARCHAR | 32 | Y | Y | Y | | | knowledge_chunks 참조 |
| 유사도 점수 | score | DECIMAL | 5,4 | Y | | | | | |

```sql
CREATE TABLE document_evidence (
    filled_id INT NOT NULL,
    chunk_id VARCHAR(32) NOT NULL,
    score DECIMAL(5,4) NOT NULL,
    PRIMARY KEY (filled_id, chunk_id),
    FOREIGN KEY (filled_id) REFERENCES filled_fields (filled_id),
    FOREIGN KEY (chunk_id) REFERENCES knowledge_chunks (chunk_id)
);
```

### 평가 결과 (EVAL_RESULTS)

| 작성일자 | 2026-06-22 | 작성자 | 뚝딱팀 |
|---|---|---|---|

**테이블명** 평가 결과 (EVAL_RESULTS) · **테이블 ID** EVAL_RESULTS
**테이블설명** KPI 측정 결과(정확도·근거율·시간단축·외부호출)를 관리한다.

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|:--:|:--:|:--:|:--:|:--:|---|
| 평가 ID | eval_id | INT | | Y | Y | | | | IDENTITY |
| 데이터셋명 | dataset | VARCHAR | 50 | Y | | | | | |
| 채움 정확도 | accuracy | DECIMAL | 4,3 | Y | | | | | |
| 시간 단축률 | time_reduction | DECIMAL | 4,3 | Y | | | | | |
| 근거 일치율 | grounded_ratio | DECIMAL | 4,3 | Y | | | | | |
| 외부 호출 수 | external_calls | INT | | Y | | | | | DEFAULT 0 |
| 측정 일시 | measured_at | TIMESTAMPTZ | | Y | | | | | CURRENT_TIMESTAMP |

```sql
CREATE TABLE eval_results (
    eval_id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    dataset VARCHAR(50) NOT NULL,
    accuracy DECIMAL(4,3) NOT NULL,
    time_reduction DECIMAL(4,3) NOT NULL,
    grounded_ratio DECIMAL(4,3) NOT NULL,
    external_calls INT DEFAULT 0 NOT NULL,
    measured_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP NOT NULL,
    PRIMARY KEY (eval_id)
);
```
